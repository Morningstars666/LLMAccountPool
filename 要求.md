角色设定
你是一位拥有全栈开发经验的资深工程师，精通 Go 语言后端开发与现代化前端界面设计。
任务目标
编写一个 LLM API 调用号池管理系统。该系统作为中间层代理，统一管理多个上游 LLM API 源，对外提供兼容 OpenAI 格式的统一 API 接口，并具备额度管控与负载均衡功能。
技术栈要求
后端：使用 Go 语言 + Gin 框架。
依赖库：后端代理逻辑必须使用 openai-go 库实现。
前端：技术栈不限，要求界面美观、现代化，具备良好的交互体验。
协议兼容：对外提供的 API 接口必须完全兼容 OpenAI 请求格式。
核心功能需求
1. 用户体系
单用户模式：系统仅服务单一管理员用户，无需复杂的多人权限管理。
基础认证：提供登录功能，支持修改账号名称和密码。
2. 模型与请求源管理（核心实体）
对外模型：用户可创建“对外模型”供客户端调用。
请求源绑定：每个“对外模型”可绑定不限量的“请求源”。
请求源结构：包含 API地址、API Key、模型名称（即上游真实模型名）。
计费配置：每个请求源支持独立配置计费模式：
模式 A：按次计费（限制调用次数）。
模式 B：按 Token 计费（限制 Input+Output Token 总量）。
用户可设定具体的额度上限。
计数逻辑：系统在代理转发过程中统计用量。
仅统计成功的请求，失败请求不扣减额度。
3. 代理密钥管理
密钥生成：系统可生成多个用于调用本系统的 API Key（供客户端使用）。
属性管理：每个 API Key 支持添加备注。
独立统计：记录每个 API Key 的调用详情（包括使用次数、Token 消耗、输出token数量、输出token数量、对应请求源等）。
4. 调度与负载均衡策略
针对每个“对外模型”，提供以下两种切换请求源的策略：
策略一：轮询切换。
策略二：用完切换。
自动熔断与切换：
当某个请求源达到设定的额度限制时，必须静默切换至下一个可用的请求源。
若该模型下所有请求源均不可用（额度耗尽或失效），则向客户端返回明确的错误信息。
5. 管理界面与可视化
仪表盘：提供直观的界面展示系统状态。
用量监控：按“对外模型”维度组织数据，展示每个请求源的：
已用量（次数/Token）。
剩余额度。
调用成功率（可选）。
非功能性需求
健壮性：程序需稳定运行，正确处理上游 API 超时或网络异常情况。
UI/UX：界面需整洁美观，操作逻辑清晰，建议使用成熟的 UI 组件库。